import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.practicalink.app',
  appName: 'PracticaLink',
  webDir: 'dist/practicalink-frontend/browser'
};

export default config;
